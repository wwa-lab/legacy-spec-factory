# Access Paths: [DATA-SLUG]

## Overview

This document catalogs all logical files (LF), SQL views, and indexes that provide access patterns to the underlying physical files and SQL tables. Access paths define how programs and queries retrieve data.

## Logical Files (DDS LF)

### [LOGICAL-FILE-NAME] (OBJ-ID)

**Source**: [SRCLIB/SRCMBR where LF DDS is defined]
**Underlying File(s)**: [PF name(s) or other LF names]
**Evidence**: [EV-ID pointing to DDS source]

**Access Key**:
```
K [FIELD1] [{A|D}]
K [FIELD2] [{A|D}]
Key Order: [field sequence and direction]
Source Line: [nn]
```

**Key Characteristics**:

| Property | Value | Evidence |
| --- | --- | --- |
| Key Uniqueness | [UNIQUE / non-unique] | DDS line nn |
| Select/Omit Logic | [DDS S/O lines or SQL WHERE clause] | DDS line nn |
| Join Type (if join LF) | [inner / left / full] | JFILE / JFLD lines |
| Member Files (if join) | [file1, file2, ...] | JFILE declaration |

**Selection Criteria** (if SELECT is used):

```
DDS S/O lines or SQL WHERE clause:
[FIELD = value OR FIELD IN (list), ...]
```

**Join Relationships** (if JFILE is used):

```
JFILE([FILE1 FILE2])
JFLD([FILE1.JOINKEY FILE2.JOINKEY])
```

**Usage Context** (from approved program analyses):

[List programs that reference this LF and how they use it: SETLL + READP, CHAIN, sequential READ, etc.]

**TBDs**:

- [ ] TBD-SLUG-NNN: [pending detail or SME confirmation]

---

## SQL Views and Indexes

### [VIEW-NAME]

**Source**: [SQL DDL file or DB2 catalog query]
**Base Tables**: [table1, table2, ...]
**View Definition**:

```sql
[SELECT ... FROM ... WHERE ... JOIN ...]
```

**Indexed Columns** (if materialized):

- [COLUMN1] (ASC / DESC)
- [COLUMN2] (ASC / DESC)

**Usage**: [which programs or reports reference this view]
**Evidence**: [EV-ID for source DDL]

---

### [INDEX-NAME]

**Source**: [DB2 catalog or SQL DDL]
**Table**: [underlying table]
**Indexed Columns**:

| Column | Sequence | Order | Evidence |
| --- | --- | --- | --- |
| [name] | [1..n] | [ASC|DESC] | EV-ID |

**Unique Index**: [Y / N]
**Status**: [active | deprecated | candidate]

---

## Access Path Summary Table

| Logical File / Index | Base File | Key Fields | Uniqueness | Programs Using | Status |
| --- | --- | --- | --- | --- | --- |
| [LF or INDEX name] | [PF name] | [field list] | [Y / N] | [prog1, prog2, ...] | [active/deprecated] |

---

## Performance Observations

[From approved program and flow analyses, note any observed performance patterns related to access paths, e.g., "CUSTBYCTY is used for region-level reports; CUSTBYPHN rarely used"]

---

## TBDs and Unresolved Access Paths

- [ ] TBD-SLUG-NNN: Pending DDS for LF [name] - selection criteria unconfirmed
- [ ] TBD-SLUG-NNN: Confirm whether index [name] is still active in production

---

Generated by legacy-ibmi-data-model-analyzer v0.1.0
Legacy Spec Factory Copyright 2026 Leo L Zhang
